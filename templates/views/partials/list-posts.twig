{% if posts %}
  {% for post in posts %}
    <div class="post">
      <a href="{{ post.link }}">
        <h3 class="post__title">
          {{ post.title }}
        </h3>
      </a>
    </div>
  {% endfor %}
{% else %}
  <p>No posts found</p>
{% endif %}